fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
google-cloud-firestore==2.20.0
google-cloud-bigquery==3.27.0
# Google Ads API client for refresh_roas_sims.py (ROAS Simulations collector).
# 31.x targets Google Ads API v25 by default — refresh_roas_sims.API_VERSION pins the
# version explicitly, so bumping this line must be paired with checking that constant.
google-ads==31.2.0
# Voyado Engage arrives over Delta Sharing (voyado_sync.py) — the Engage REST
# API has no campaign-statistics endpoints, so this is the only route to sends,
# opens and clicks. pandas/pyarrow come in as its dependencies but are pinned
# explicitly because voyado_sync also needs them directly for
# BigQuery load_table_from_dataframe.
delta-sharing==1.4.2
pandas==2.3.3
pyarrow==23.0.1
